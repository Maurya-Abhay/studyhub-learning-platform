'use client';
import Link from 'next/link';
import { useMemo, useState } from 'react';
import { ArrowRight, Search } from 'lucide-react';
import type { DsaProblem } from '@/types';
export function ProblemList({problems,solvedIds=[]}:{problems:DsaProblem[];solvedIds?:string[]}){const [difficulty,setDifficulty]=useState('All');const [pattern,setPattern]=useState('All');const [status,setStatus]=useState('All');const [query,setQuery]=useState('');const solved=new Set(solvedIds);const patterns=Array.from(new Set(problems.map(p=>p.pattern).filter(Boolean))).sort();const filtered=useMemo(()=>problems.filter(p=>(difficulty==='All'||p.difficulty===difficulty)&&(pattern==='All'||p.pattern===pattern)&&(status==='All'||status==='Solved'&&solved.has(p.id)||status==='Unsolved'&&!solved.has(p.id))&&(`${p.title} ${p.summary}`.toLowerCase().includes(query.toLowerCase()))),[problems,difficulty,pattern,status,query,solvedIds]);return <div className="surface card"><div className="filter-bar"><label className="search" style={{flex:1,minWidth:210}}><Search size={15}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search problems..."/></label><select className="filter-select" value={difficulty} onChange={e=>setDifficulty(e.target.value)}><option>All</option><option>Easy</option><option>Medium</option><option>Hard</option></select><select className="filter-select" value={pattern} onChange={e=>setPattern(e.target.value)}><option>All</option>{patterns.map(p=><option key={p}>{p}</option>)}</select><select className="filter-select" value={status} onChange={e=>setStatus(e.target.value)}><option>All</option><option>Solved</option><option>Unsolved</option></select></div><div className="problem-list">{filtered.length?filtered.map(problem=><Link key={problem.id} href={`/dsa/problem/${problem.slug}`} className="problem-row"><div><strong>{solved.has(problem.id)?'✓ ':''}{problem.title}</strong><div className="small muted">{problem.summary}</div></div><div className="meta-row"><span className={`pill ${problem.difficulty.toLowerCase()}`}>{problem.difficulty}</span>{problem.pattern&&<span className="chip active">{problem.pattern}</span>}</div><ArrowRight size={14} color="var(--muted)"/></Link>):<div className="empty">No problems match these filters.</div>}</div></div>}
