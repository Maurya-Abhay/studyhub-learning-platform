'use client';
import Link from 'next/link';
import { useState } from 'react';
import { useRouter } from 'next/navigation';
import { createBrowserClient } from '@supabase/ssr';
import { LogIn } from 'lucide-react';
const supabaseUrl=process.env.NEXT_PUBLIC_SUPABASE_URL; const supabaseKey=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY;
export default function Login(){const router=useRouter();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [msg,setMsg]=useState('');const [busy,setBusy]=useState(false); async function submit(e:React.FormEvent){e.preventDefault(); if(busy)return; setMsg(''); if(!supabaseUrl||!supabaseKey){setMsg('Supabase is not configured. Add your environment variables to enable login.'); return;} setBusy(true); const supabase=createBrowserClient(supabaseUrl,supabaseKey); const {data,error}=await supabase.auth.signInWithPassword({email,password}); if(error){setMsg(error.message);setBusy(false);return;} const {data:profile}=await supabase.from('profiles').select('role').eq('id',data.user.id).maybeSingle(); if(profile?.role==='admin'){router.replace('/admin');return;} if(profile?.role==='user'){router.replace('/dashboard');return;} await supabase.auth.signOut();setMsg('Your account role is not configured. Contact an administrator.');setBusy(false);} return <main className="auth-wrap"><div className="surface auth-card"><div className="auth-brand"><div className="brand"><span className="brand-mark"><LogIn size={17}/></span>StudyHub</div><h1 className="title" style={{marginTop:22}}>Welcome back.</h1><p className="subtitle">Login to continue your learning plan and keep progress synced.</p></div><form onSubmit={submit} style={{display:'grid',gap:12}}><div className="field"><label>Email</label><input value={email} onChange={e=>setEmail(e.target.value)} type="email" required placeholder="you@example.com"/></div><div className="field"><label>Password</label><input value={password} onChange={e=>setPassword(e.target.value)} type="password" required placeholder="••••••••"/></div>{msg&&<div className="notice">{msg}</div>}<button className="btn primary" type="submit" disabled={busy}>{busy?'Signing in...':'Login'}</button></form><p className="small muted" style={{marginTop:16}}>New here? <Link className="link" href="/register">Create an account</Link></p></div></main>}
