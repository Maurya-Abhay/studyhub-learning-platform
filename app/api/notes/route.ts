import { NextResponse } from 'next/server';
import { createClient } from '@/lib/supabase/server';
export async function POST(req:Request){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:'Sign in required.'},{status:401});let b:any;try{b=await req.json()}catch{return NextResponse.json({error:'Invalid JSON.'},{status:400})};const topicId=typeof b.topicId==='string'?b.topicId:'';const content=typeof b.content==='string'?b.content:'';if(!topicId)return NextResponse.json({error:'Topic is required.'},{status:400});const{data,error}=await supabase.from('personal_notes').upsert({user_id:user.id,topic_id:topicId,content,updated_at:new Date().toISOString()},{onConflict:'user_id,topic_id'}).select().single();if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({note:data});}
