'use client';
import Link from 'next/link';
import { useState } from 'react';
import { useRouter } from 'next/navigation';
import { createBrowserClient } from '@supabase/ssr';
export default function Register(){const router=useRouter();const [name,setName]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [msg,setMsg]=useState('');const [busy,setBusy]=useState(false); async function submit(e:React.FormEvent){e.preventDefault(); if(busy)return; if(!process.env.NEXT_PUBLIC_SUPABASE_URL||!process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY){setMsg('Supabase is not configured. Add your environment variables to enable registration.'); return;} setMsg(''); setBusy(true); const supabase=createBrowserClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY); const {data,error}=await supabase.auth.signUp({email,password,options:{data:{name}}}); if(error){setMsg(error.status===429?'Supabase signup limit reached. Wait for the limit window to reset, then try again.':error.message);setBusy(false);return;} if(data.session){router.replace('/dashboard');return;} setMsg('Account created. Turn off email confirmation in Supabase to log in immediately.');setBusy(false);} return <main className="auth-wrap"><div className="surface auth-card"><h1 className="title">Create your account.</h1><p className="subtitle">Start courses, save progress and build a personal study plan.</p><form onSubmit={submit} style={{display:'grid',gap:12,marginTop:20}}><div className="field"><label>Name</label><input value={name} onChange={e=>setName(e.target.value)} required/></div><div className="field"><label>Email</label><input value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></div><div className="field"><label>Password</label><input value={password} onChange={e=>setPassword(e.target.value)} type="password" minLength={8} required/></div>{msg&&<div className="notice">{msg}</div>}<button className="btn primary" type="submit" disabled={busy}>{busy?'Creating account...':'Create account'}</button></form><p className="small muted" style={{marginTop:16}}>Already have an account? <Link className="link" href="/login">Login</Link></p></div></main>}
